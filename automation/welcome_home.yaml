- alias: Welcome home
  trigger:
    - platform: state
      entity_id: device_tracker.henry_phone
      from: "not_home"
      to: "home"
      for: "00:02:00"

  action:
    - service: script.tts
      data_template:
        value1: >
          Henry, {{ [
          "Welcome home",
          "Welcome back"
          ] | random }}
